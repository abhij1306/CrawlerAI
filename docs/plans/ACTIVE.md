# Active Plan

**Current:** Extraction Correctness Overhaul — Clean Foundation → Compiled Recipes → Learned Fallback → `docs/plans/crawlerai-adaptive-extraction-v2-plan.md`
**Status:** IN PROGRESS — Phase 5 fail-closed runtime implementation is present and awaiting the single final verification run. Phase 4 still has no passing candidate benchmark, so no production artifact is active.
**Started:** 2026-07-02
**Notes:** Single authoritative extraction roadmap. Sequence: 0 clean foundation and baseline → 1 deterministic contracts → 2 compiled recipe fast path → 3 grounded operator correction → 4 offline universal-extractor research/evaluation → 5 evaluation-gated runtime ML fallback → 6 Sentinel → 7 grounded LLM repair. Product/ops/governance remains deferred. Phase 4 uses `compact_page.v2` and `universal_model_benchmark.v2`; review hardening closed truth leakage, source-grounding, contamination, baseline-validation, and reproducibility defects. On 2026-07-02 the operator explicitly said to continue after the missing-benchmark blocker. Phase 5 therefore implements a fail-closed runtime path without fabricating approval: recipe → generic deterministic → lazy model fallback → normal Resolve/Publish. Runtime requires approved passing benchmark metadata in the frozen snapshot and exact adapter/artifact identity. Missing/unapproved artifacts disable cleanly. Grounded predictions become Evidence only; source-path/value checks, timeout/degraded outcomes, memory/cost bounds, diagnostics, and per-result invocation/latency/grounding/cost metrics are present. No serving process was added because Phase 4 selected `no_production_serving`. The existing deterministic attribute-spelling recovery remains model-free. `job_resolution.py` was consolidated into `jobs.py` to keep the extraction package at 24 modules. Final verification remains pending by operator instruction.
